---
local:
  hosts:
    localhost:
      ansible_host: localhost
      ansible_connection: local
project:
  hosts:
    nexus-01:
#      ansible_host: <nexushost>
      ansible_host: 158.160.137.206
  children:
    nexus:
      hosts:
        nexus-01:
  vars:
    ansible_connection_type: paramiko
    ansible_user: eurus_cloud